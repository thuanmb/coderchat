<div class="inner cover">
    <h1 class="cover-heading">Chatting Tool</h1>
    <p class="lead">Chat anywhere, in anytime</p>
    <p class="lead">
        <%= link_to 'Sign Up Free', signup_path, {:class => 'btn btn-md btn-warning'}%>
        <%= link_to 'Login', login_path, {:class => 'btn btn-md btn-primary'}%>
        <%= link_to "Sign in with Facebook", "/auth/facebook", {:class => 'btn btn-md btn-info'} %>
    </p>
</div>

<%= javascript_tag do%>
    $(".nav-masthead .nav-link.home").addClass("active");
<% end %>